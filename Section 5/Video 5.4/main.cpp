#include <iostream>
#include <cstdlib>
#include <cmath>

using namespace std;

int main()
{
    int a = 10, b = -20;
    double x = 1.45, y = 3.5;
    cout << std::abs(b) << endl;
    cout << std::sqrt(y) << endl;
    cout << std::exp(x) << endl;
    cout << std::pow(x, y) << endl;
    cout << std::sin(x) << endl;
    return 0;
}
